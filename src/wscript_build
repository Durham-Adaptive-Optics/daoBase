#! /usr/bin/env python

bld.shlib(
	source = 'c/daoBase.c',
	includes = '../include',
	cflags = ['-Ofast', '-Wall', '-Wextra', '-pipe'],
	target = 'daoBase',
	name   = 'daoBase',
	vnum   = '0.0.1')


