[package]
name = "rust"
version = "0.1.0"
edition = "2021"

[dependencies]

[build-dependencies]
bindgen = "0.70.1"  # Use the latest version of bindgen
cc = "1"  # Required to compile C code if needed

# rustflags (link path to libdao) are set in .cargo/config.toml